# Public values only. Never put a Supabase service-role key in this file or in Vite.
# Copy these values from Supabase Dashboard → Project Settings → API.
# The anon (publishable) key is safe for a browser client; database access is enforced by RLS.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# VITE_BASE_PATH=/
